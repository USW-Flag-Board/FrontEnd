import { styled } from "@mui/material/styles";

const WriteTitle = styled("h1")({
  marginBottom: 10,
});

const test = () => {
  return (
    <WriteTitle>알고리즘 (코테반)</WriteTitle>
  );
}

export default test;
